<h1>Candidates</h1>

<% @candidates.each do |candidate| %>
  <ul>
    <li><%= link_to candidate.last_name, candidate %>, <%= link_to candidate.first_name, candidate %></li>
    <li><%= candidate.email %></li>
  </ul>
<% end %>
